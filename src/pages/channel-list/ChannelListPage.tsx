import './ChannelListPage.scss';
import { bem } from '@shared/libs';

const b = bem('channel-list-page');


const ChannelListPage = () => {
  return (
    <div className={b()}>

    </div>
  );
};

export default ChannelListPage;